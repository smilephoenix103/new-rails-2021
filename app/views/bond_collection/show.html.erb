<div class="container">
	<div class="container row">
      <div class="col-md-2">
		<%= link_to (render 'renders/button_back'), bond_collection_index_path, class:"btn btn-secondary btn-sm" %> 
		<h3>WALUTY</h3>
      </div>
      <div class="col-md-8">
        <%= render 'renders/show_flag_country' %>
      </div>			
      <div class="col-md-2">
      </div>
    </div>
</div>
<% @currencies.each do |currency| %>
	<div class="btn btn-secondary btn-sm" style="margin:10px;">
		<%= link_to (currency.data_exchange), show_bonds_path(currency), style:"color:black; margin:10px; padding:10px" %>
		
	</div>
<% end %>