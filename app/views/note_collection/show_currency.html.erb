<h3>WALUTY</h3>
<% @currencies.each do |currency| %>
	<div class="btn btn-secondary btn-sm" style="margin:10px;">
		<%= link_to (currency.id.to_s + " - " + currency.data_exchange + ' | ' + currency.currency_series.to_s), show_note_path(currency), style:"color:black; margin:10px; padding:10px" %>
		
	</div>
<% end %>