
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<%= link_to 'Pecunia', root_path, class:"navbar-brand" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    <% if user_signed_in? && current_user.role == 'admin' %>
    <li class="nav-item">          
            <div class="dropdown nav-link ">
              <button class="dropbtn bg-dark dropdown-toggle">Country</button>
                <div class="dropdown-content">
                  <%= link_to 'Add Country', new_country_path, class:"nav-link" %>
                  <%= link_to 'Countries', countries_path, class:"nav-link" %>             
                </div>
            </div>        
        </li>
    <% end %> 
 
      <li class="nav-item " > 
        <% if user_signed_in? %>
          <div class="dropdown nav-link ">
            <button class="dropbtn bg-dark dropdown-toggle">Notes</button>
            <div class="dropdown-content ">
              <% if user_signed_in? && current_user.role == 'admin' %>
                <%= link_to 'Add Note', notes_path, class:"nav-link" %>
                <%= link_to 'Note Sold', note_sold_index_path, class:'nav-link' %>
              <% end %>
              <% if user_signed_in? %>
                  <%= link_to 'Note For Sell', note_for_sell_index_path, class:"nav-link" %>
                  <%= link_to 'Collection', note_collection_index_path, class:"nav-link" %>
              <% end %>
                
            </div>
          </div>
        <% end %>
      </li>

      <li class="nav-item"> 
        <% if user_signed_in? %>
          <div class="dropdown nav-link ">
            <button class="dropbtn bg-dark dropdown-toggle">Coins</button>
              <div class="dropdown-content">
              <% if user_signed_in? && current_user.role == 'admin' %>
                <%= link_to 'Add Coin', coins_path, class:"nav-link" %>             
              <% end %>
              <% if user_signed_in? %>
                 <%= link_to "Coin For Sell", coin_for_sell_index_path, class:"nav-link" %>
                 <%= link_to 'Collection', coin_collection_index_path, class:"nav-link" %>
              <% end %>
                
            </div>
          </div>
        <% end %>
      </li>

       <li class="nav-item"> 
        <% if user_signed_in? && current_user.role == 'admin' %>
          <div class="dropdown nav-link ">
            <button class="dropbtn bg-dark dropdown-toggle">Bonds</button>
              <div class="dropdown-content">
              <% if user_signed_in? && current_user.role == 'admin' %>
                <%= link_to 'Add Bond', bonds_path, class:"nav-link" %>             
              <% end %>
              <% if user_signed_in? %>
                <%
=begin%>
 <%= link_to "Coin For Sell", coin_for_sell_index_path, class:"nav-link" %> 
<%
=end%>
                <%= link_to 'Collection', bond_collection_index_path, class:"nav-link" %> 
              <% end %>
                
            </div>
          </div>
        <% end %>
      </li>

        <li class="nav-item"> 
          <% if user_signed_in? && current_user.role == 'admin' %>
            <div class="dropdown nav-link ">
              <button class="dropbtn bg-dark dropdown-toggle">Reports</button>
                <div class="dropdown-content">
                  <%= link_to 'Reports', reports_path, class:"nav-link" %>  
                  <%= link_to 'Chart', reports_chart_path, class:"nav-link" %>              
                  <hr class="my-1">
                  <%= link_to 'Add Pattern', patterns_path, class:"nav-link" %>
                  <%= link_to 'Add bought', boughts_path, class:"nav-link" %>
                </div>
            </div>
          <% end %>
        </li>

      
      
      <li class="nav-item">
      	<%= link_to 'About PECUNIA', home_about_path, class:"nav-link" %>
      </li>
      
    </ul>
    <%
=begin%>
 <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form> 
<%
=end%>

    <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
            <li class= "navbar-brand">
              <%= current_user.role %>
            </li>
            
        
              <li class="nav-item">
                <%= link_to current_user.email, edit_user_registration_path, class:"nav-link" %>
              </li>
           
            
            <li class="nav-item">
              <%= button_to "Logout", destroy_user_session_path, method: :delete, class:"btn btn-sm btn-outline-secondary"  %>
            </li>
        <% else %>
          <li class="nav-item"> 
            <%= link_to "Sign Up", new_user_registration_path, class:"nav-link" %>
          </li> 
          <li class="nav-item"> 
            <%= link_to "Sign In", new_user_session_path, class:"nav-link" %>
          </li> 
        <% end %>
      </ul> 
  </div>
</nav>

<!--
<script>
/* When the user clicks on the button, 
toggle between hiding and showing the dropdown content */
function myFunction() {
  document.getElementById("myDropdown").classList.toggle("show");
}
function myFunctionCoin() {
  document.getElementById("myDropdownCoin").classList.toggle("show");
}
// Close the dropdown if the user clicks outside of it
window.onclick = function(event) {
  if (!event.target.matches('.dropbtn')) {
    var dropdowns = document.getElementsByClassName("dropdown-content");
    var i;
    for (i = 0; i < dropdowns.length; i++) {
      var openDropdown = dropdowns[i];
      if (openDropdown.classList.contains('show')) {
        openDropdown.classList.remove('show');
      }
    }
  }
}
</script>
-->