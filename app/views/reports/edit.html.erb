<h1>Editing Report</h1>

<%= render 'form', report: @report %>
<%# report = @report %>

<%= link_to render('renders/button_show'), @report, {class:"btn btn-info btn-sm", 'data-toggle' => "modal", 'data-target' => "#" + "R"+ @report.id.to_s} %>
  <div class="modal fade" id=<%= "R" + @report.id.to_s %> data-backdrop="static" data-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <%= render 'show_modal', report: @report %>
  </div>
<%= link_to render('renders/button_back'), reports_path, class: "btn btn-secondary btn-sm" %>
